想实现在一个选品的操作功能服务。主要需要的功能有

1. 更新cookie等参数
2. 执行采集任务
3. 数据sql执行框
4. 数据加载表格

---

```
CREATE TABLE douyin_products (
    promotion_id VARCHAR(255),
    product_id VARCHAR(255),
    title TEXT,
    cover JSON,
    detail_url TEXT,
    promotion_source INT,
    price DECIMAL(10, 2),
    market_price DECIMAL(10, 2),
    cos_fee DECIMAL(10, 2),
    cos_ratio DECIMAL(5, 2),
    month_sales BIGINT,
    shop_id BIGINT,
    shop_name VARCHAR(255),
    exp_score VARCHAR(10),
    in_promotion BOOLEAN,
    sales BIGINT,
    tag_list JSON,
    text_product_tag JSON,
    has_same_type BOOLEAN,
    is_show_score BOOLEAN,
    board_data JSON,
    price_text VARCHAR(50),
    rank_type VARCHAR(255),  -- 新增字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP  -- 新增创建时间字段
);


CREATE TABLE douyin_products_month (
    promotion_id VARCHAR(255),
    product_id VARCHAR(255),
    title TEXT,
    cover JSON,
    detail_url TEXT,
    promotion_source INT,
    price DECIMAL(10, 2),
    market_price DECIMAL(10, 2),
    cos_fee DECIMAL(10, 2),
    cos_ratio DECIMAL(5, 2),
    month_sales BIGINT,
    shop_id BIGINT,
    shop_name VARCHAR(255),
    exp_score VARCHAR(10),
    in_promotion BOOLEAN,
    sales BIGINT,
    tag_list JSON,
    text_product_tag JSON,
    has_same_type BOOLEAN,
    is_show_score BOOLEAN,
    board_data JSON,
    price_text VARCHAR(50),
    rank_type VARCHAR(255),  -- 新增字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP  -- 新增创建时间字段
);

```

